w-1 = 0.25rem = 4px
w-xs = 20rem = 320px
w-xl =36rem = 576px
w-2xl = 42rem =672px
w-3xl....


what if we put width greater than max-width

Answer:- If you set a width (w-*) greater than the max-width (max-w-*) in Tailwind CSS, the max-width will take precedence and constrain the element's width. This means the element's width will not exceed the max-width value, even if the width value is larger.